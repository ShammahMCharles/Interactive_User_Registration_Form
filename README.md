Features
  Create new blog posts
  Add a title and content for each post
  Edit existing posts
  Delete posts
  Form validation using HTML5 required fields
  Disabled submit button until all required fields are completed
  Dynamic DOM manipulation using JavaScript
  Responsive two-column layout
  User profile image displayed with each post
  Foundation for browser persistence using Local Storage
Form Validation
  HTML required attributes
  Enabling/disabling the submit button based on user input
  Resetting the form after successful submission
  Challenges/fixed bugs:
    Selecting the correct DOM elements
    Dynamically creating HTML elements
    Working with event listeners on dynamically created content
    Debugging DOM selection issues
    Properly appending newly created elements to the page
    Updating existing DOM elements during editing
    Managing button state after form submission
